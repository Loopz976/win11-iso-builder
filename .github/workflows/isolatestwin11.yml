name: Build Windows 11 ISO (Final Clean)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # 📦 Extrair arquivos
      - name: Extract files
        run: |
          tar -xf script.zip

      # 🔧 Patch script (remover interação do CHOICE)
      - name: Patch script (auto mode)
        shell: powershell
        run: |
          $file = Get-ChildItem -Filter *.cmd | Select-Object -First 1
          $content = Get-Content $file.FullName
          $content = $content -replace 'CHOICE /c fs.*', 'set ListSel=F'
          $content | Set-Content "script_auto.cmd"

      # 🚀 Executar script (agora sem erro final)
      - name: Run script
        shell: cmd
        run: |
          call script_auto.cmd

      # 📂 Criar pasta output
      - name: Create output folder
        run: mkdir output

      # 🔥 Processar ISOs (1 por vez, só PT-BR)
      - name: Process ISOs safely
        shell: cmd
        run: |
          for %%f in (*.iso) do (
            echo %%f | findstr /i "pt-br" >nul
            if not errorlevel 1 (
              echo Processando %%f

              move "%%f" output\

              cd output

              echo Compactando %%f
              7z a -v1900m -mx=0 "%%~nf.7z" "%%f"

              echo Removendo ISO
              del "%%f"

              cd ..
            )
          )

      # 🔥 Deletar release antigo (overwrite real)
      - name: Delete old release
        continue-on-error: true
        run: |
          gh release delete win11-23h2-22631.6060 --yes

      # 🚀 Upload para release
      - name: Upload Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: win11-23h2-22631.6060
          name: Windows 11 23H2 22631.6060 PT-BR
          files: output/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
