name: LTSC AUTO BUILD (FINAL STABLE)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-2022

    steps:

    # ================================
    # 🔹 CACHE ISO BASE
    # ================================
    - name: Cache ISO
      id: cache-iso
      uses: actions/cache@v4
      with:
        path: source.iso
        key: ltsc-iso-v1

    # ================================
    # 🔥 DOWNLOAD ISO (RÁPIDO)
    # ================================
    - name: Baixar ISO partes
      if: steps.cache-iso.outputs.cache-hit != 'true'
      shell: powershell
      run: |
        curl.exe -L -o part.7z.001 https://github.com/Loopz976/ltsc-auto-builder/releases/download/w10-ltsc-2021-24459629943/W10LTSC2021.7z.001
        curl.exe -L -o part.7z.002 https://github.com/Loopz976/ltsc-auto-builder/releases/download/w10-ltsc-2021-24459629943/W10LTSC2021.7z.002
        curl.exe -L -o part.7z.003 https://github.com/Loopz976/ltsc-auto-builder/releases/download/w10-ltsc-2021-24459629943/W10LTSC2021.7z.003

    - name: Instalar 7zip
      run: choco install 7zip -y

    - name: Extrair ISO base
      if: steps.cache-iso.outputs.cache-hit != 'true'
      shell: powershell
      run: |
        & "C:\Program Files\7-Zip\7z.exe" x part.7z.001 -aoa

    - name: Padronizar ISO
      if: steps.cache-iso.outputs.cache-hit != 'true'
      shell: powershell
      run: |
        $iso = Get-ChildItem *.iso | Select-Object -First 1
        Rename-Item $iso.Name source.iso

    # ================================
    # 🔥 EXTRAIR ISO
    # ================================
    - name: Criar work
      run: mkdir work

    - name: Extrair ISO
      shell: powershell
      run: |
        & "C:\Program Files\7-Zip\7z.exe" x source.iso -owork -aoa

    - name: Remover ISO pra liberar espaço
      run: del source.iso

    # ================================
    # 🔥 BACKUP WIM
    # ================================
    - name: Backup original
      run: copy work\sources\install.wim work\sources\install_original.wim

    # ================================
    # 🔥 DOWNLOAD UPDATES (CAB REAL)
    # ================================
    - name: Criar pasta updates
      run: mkdir updates

    - name: Baixar updates
      shell: powershell
      run: |
        cd updates

        # ⚠️ COLOQUE LINKS REAIS AQUI
        curl.exe -L -o SSU.cab LINK1
        curl.exe -L -o KB1.cab LINK2
        curl.exe -L -o KB2.cab LINK3
        curl.exe -L -o KB3.cab LINK4
        curl.exe -L -o KB4.cab LINK5
        curl.exe -L -o KB5.cab LINK6
        curl.exe -L -o KB6.cab LINK7

    # ================================
    # 🔥 MOUNT
    # ================================
    - name: Criar mount
      run: mkdir mount

    - name: Montar imagem
      shell: powershell
      run: |
        dism /Mount-Wim /WimFile:work\sources\install.wim /index:1 /MountDir:mount

    # ================================
    # 🔥 INTEGRAR (SSU PRIMEIRO)
    # ================================
    - name: Integrar SSU
      shell: powershell
      run: |
        dism /Image:mount /Add-Package /PackagePath:updates\SSU.cab

    - name: Integrar restantes
      shell: powershell
      run: |
        dism /Image:mount /Add-Package /PackagePath:updates

    # ================================
    # 🔥 CLEAN HARD
    # ================================
    - name: Cleanup
      shell: powershell
      run: |
        dism /Image:mount /Cleanup-Image /StartComponentCleanup /ResetBase

    # ================================
    # 🔥 UNMOUNT
    # ================================
    - name: Commit
      shell: powershell
      run: |
        dism /Unmount-Wim /MountDir:mount /Commit

    # ================================
    # 🔥 REMOVER TEMP PESADO
    # ================================
    - name: Limpar updates
      run: rmdir /s /q updates

    # ================================
    # 🔥 CONVERTER ESD
    # ================================
    - name: Converter para ESD
      shell: powershell
      run: |
        dism /Export-Image /SourceImageFile:work\sources\install.wim `
        /SourceIndex:1 `
        /DestinationImageFile:work\sources\install.esd `
        /Compress:recovery

        del work\sources\install.wim

    # ================================
    # 🔥 CRIAR ISO
    # ================================
    - name: Instalar ADK
      run: choco install windows-adk -y

    - name: Criar ISO
      shell: powershell
      run: |
        $oscdimg = "C:\Program Files (x86)\Windows Kits\10\Assessment and Deployment Kit\Deployment Tools\amd64\Oscdimg\oscdimg.exe"
        & $oscdimg -bwork\boot\etfsboot.com -u2 -h -m -o -lLTSC_FINAL work final.iso

    # ================================
    # 🔥 LIMPAR WORK (ANTES DO SPLIT)
    # ================================
    - name: Limpar work
      run: rmdir /s /q work

    # ================================
    # 🔥 SPLIT 1900MB
    # ================================
    - name: Split ISO
      shell: powershell
      run: |
        & "C:\Program Files\7-Zip\7z.exe" a split.7z final.iso -v1900m

    - name: Remover ISO
      run: del final.iso

    # ================================
    # 📦 UPLOAD FINAL
    # ================================
    - name: Upload partes
      uses: actions/upload-artifact@v4
      with:
        name: LTSC-FINAL
        path: split.7z.*
